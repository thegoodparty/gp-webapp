import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { renderColorSwatches } from './colors.mdx';

<Meta title="Theme/Theme Colors" />
<ColorPalette>
  {renderColorSwatches(
    'primary',
    'secondary',
    'tertiary',
    'neutral',
    'error',
    'success',
    'warning',
    'info',
  )}
</ColorPalette>
