const DEFAULT_PAGE_SIZE = 20

const PAGE_SIZES = [10, 20, 50, 100]

const SHEET_MODES = {
  CREATE: 'create',
  EDIT: 'edit',
} as const

const ALL_SEGMENTS = 'all'

export { DEFAULT_PAGE_SIZE, PAGE_SIZES, SHEET_MODES, ALL_SEGMENTS }
