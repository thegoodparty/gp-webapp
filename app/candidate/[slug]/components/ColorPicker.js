export default function ColorPicker({ color }) {
  return (
    <div
      className="inline-block px-8 py-4 text-lg font-black bg-violet-100 rounded-full"
      style={{ color }}
    >
      color
    </div>
  );
}
