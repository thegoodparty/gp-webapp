export default function GetInvolvedButton({ color }) {
  return (
    <div
      className="inline-block px-8 py-4 text-lg font-black bg-white rounded-full"
      style={{ color }}
    >
      GET INVOLVED
    </div>
  );
}
