import { createTheme } from '@mui/material/styles';

const materialTheme = createTheme({
  palette: {
    primary: {
      main: '#8257FF', // Your desired color
    },
  },
});

export default materialTheme;
